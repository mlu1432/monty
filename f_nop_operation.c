#include "monty.h"

/**
 * f_nop - Does nothing.
 * @top: Pointer to the top of the stack.
 * @counter: Line number.
 * Return: Nothing.
 */
void f_nop(stack_t **top, unsigned int counter)
{
	(void) counter;
	(void) top;
}
